```
  // Start project

  copy .env.example -> .env

  yarn install
  yarn dev -> start in development
  yarn build -> build project vanilla js
  yarn start -> start in product

  yarn pretty -> format all code
  yarn node-sass -> compiler sass/scss to css
```